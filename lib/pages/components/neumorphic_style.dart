import 'package:flutter_neumorphic/flutter_neumorphic.dart';

class CustomNeumorphic extends StatelessWidget {
  const CustomNeumorphic({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
